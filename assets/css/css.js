import { StyleSheet } from "react-native";

export {css};

const css = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      justifyContent: 'center',
    },
    mapa:{
      height: '60%',
    },
    pesc:{
      height: '40%',
    },
    
  });